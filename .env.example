# This project needs no environment variables to run or to deploy on Vercel.
#
# Optional: override the API base URL the frontend calls. Leave unset for local
# development and for Vercel (both use /api).
# VITE_API_URL=
#
# Optional: change the local API port (development only).
# PORT=5000
